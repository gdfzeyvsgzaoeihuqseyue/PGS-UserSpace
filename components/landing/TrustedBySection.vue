<template>
  <section class="py-16 bg-WtBAct">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center">
        <h2 class="text-base font-semibold text-primary tracking-wide uppercase">{{ $t('indexPage.trustedBy.title') }}
        </h2>
        <div class="mt-8 flex justify-center gap-8 md:gap-16 grayscale opacity-60">
          <!-- Placeholders for partner logos -->
          <div class="flex items-center space-x-2">
            <div class="h-8 w-8 bg-gray-400 rounded-full"></div>
            <span class="text-xl font-bold text-gray-400">Acme Corp</span>
          </div>
          <div class="flex items-center space-x-2">
            <div class="h-8 w-8 bg-gray-400 rounded-full"></div>
            <span class="text-xl font-bold text-gray-400">Global Tech</span>
          </div>
          <div class="flex items-center space-x-2">
            <div class="h-8 w-8 bg-gray-400 rounded-full"></div>
            <span class="text-xl font-bold text-gray-400">Nebula</span>
          </div>
          <div class="flex items-center space-x-2">
            <div class="h-8 w-8 bg-gray-400 rounded-full"></div>
            <span class="text-xl font-bold text-gray-400">Trio</span>
          </div>
        </div>
      </div>
    </div>
  </section>
</template>

<script setup lang="ts">
// Static component for social proof/trust
</script>
